def yolo_cnn(img):
    """
    Parameters:
        img - img we are trying to detect object 0. Size (0,0,3)
    Returns:
        yoloimg - smaller img with cells. Size (0,0,2,8) = (0,0,16). Each cell contains (p_c, b_x, b_y, b_h, b_w, c_1, c_2, c_3, p_c, b_x, b_y, b_h, b_w, c_1, c_2, c_3)
    """
    pass 

